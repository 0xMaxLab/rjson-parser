using System;
using main.core;

namespace main.coreore
{
    // Replaces the @FunctionalInterface from Java
    public delegate object RichJsonCommand(RichJsonParser parser, RichJsonContext context);
}